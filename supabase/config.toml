# Supabase local development configuration (Phase 1 foundation).
# Do not store secrets in this file.
#
# Apply migrations only to a verified staging/development Supabase project.
# See supabase/migrations/README.md before running `supabase db push`.

project_id = "investment-os-local"

[api]
enabled = true
port = 54321
schemas = ["public"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

[db]
port = 54322
major_version = 15

[studio]
enabled = true
port = 54323
